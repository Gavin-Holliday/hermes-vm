[defaults]
interpreter_python = /opt/homebrew/bin/python3.14

[ssh_connection]
# Pipelining avoids spawning an interactive login shell on the remote host,
# which prevents OSC/terminal escape sequences from leaking into Ansible's
# JSON response parsing. Also speeds up task execution significantly.
pipelining = true
